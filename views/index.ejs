<h2>Halo <%= name %></h2>
<% if (mahasiswa.length === 0) { %>
<p>DataMahasiswa 0</p>
<% } %>
<h4>
    <% mahasiswa.forEach(mhs => { %>
    <ul>
        <li>NIM: <%= mhs.nim %> </li>
        <li>Nama: <%= mhs.name %> </li>
    </ul>
    <% }) %>

</h4>